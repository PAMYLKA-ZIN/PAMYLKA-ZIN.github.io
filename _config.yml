title: PAMYŁKA ZIN
author:
  name: PAMYLKA-ZIN
  email: "pamylka.zin@gmail.com"

# The `>` after `description:` means to ignore line-breaks until next key.
# If you want to omit the line-break after the end of text, use `>-` instead.
description: >
  Navukova-papularny zin na biełaruskaj movie,
  jaki raspaŭsiudžvajecca pad svabodnymi licenzijami.

# Build settings

# If you clone the Minima repo and build locally, use this setting.
#theme: minima

# As of November 2023, GitHub Pages still uses Minima 2.5.1 (https://pages.github.com/versions/).
# If you want to use the latest Minima version on GitHub Pages, use the following setting and
# add a line comment on "theme: minima" above.
remote_theme: jekyll/minima

plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-remote-theme

# Theme-specific settings

# If you want to link only specific pages in your header, use this and add the path to the pages
# in order as they should show up.
header_pages:
  - _pages/read.md
  - _pages/about.md

include:
  - _pages
  - _posts
  - _drafts

exclude:
  - .gitignore
  - docker-compose.yml
  - Gemfile
  - Gemfile.lock
  - LICENSE
  - README.md

# Set to `true` to show excerpts on the homepage.
#show_excerpts: false

# Minima specific settings, which are only available from Minima 3.0 onward.
minima:
  # Minima skin selection.
  # Available skins are:
  # classic             Default, light color scheme.
  # dark               Dark variant of the classic skin.
  # auto               Adaptive skin based on the default classic and dark skins.
  # solarized-light     Light variant of solarized color scheme.
  # solarized-dark     Dark variant of solarized color scheme.
  # solarized           Adaptive skin for solarized color scheme skins.
  skin: auto

  # Minima date format.
  # The default value is "%b %d, %Y" (e.g. Nov 14, 2023)
  # Refer to https://shopify.github.io/liquid/filters/date/ if you want to customize this.
  #date_format: "%b-%d-%Y"

  # Generate social links in footer.
  social_links:
    - title: Pamyłka Zin · GitHub
      icon: github
      url: "https://github.com/PAMYLKA-ZIN"
    - title: PAMYŁKA ZIN • Instagram
      icon: instagram
      url: "https://www.instagram.com/pamylka.zin/"
    - title: PAMYŁKA ZIN - Cуполка беларусаў у Mastodon
      icon: mastodon
      url: "https://vkl.world/@PamylkaZin"
    - title: PAMYŁKA ZIN • X (Twitter)
      icon: x-twitter
      url: "https://x.com/PamylkaZin"
    - title: Прэмія Бізон Гіґс (@bison.higgs) • Instagram
      icon: instagram
      url: "https://www.instagram.com/bison.higgs/"
